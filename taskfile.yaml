---
version: 3

tasks:
  build:
    vars:
      OUTPUT_BIN: $HOME/.bin
    cmds:
      - mkdir .bin || true
      - go run tools/gen.go --output={{.OUTPUT_BIN}} --module=calculator
      - go run tools/gen.go --output={{.OUTPUT_BIN}} --module=time
      - go build -o {{.OUTPUT_BIN}}/hack cmd/main.go
